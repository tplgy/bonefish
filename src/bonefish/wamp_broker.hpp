#ifndef BONEFISH_WAMP_BROKER_HPP
#define BONEFISH_WAMP_BROKER_HPP

namespace bonefish {

class wamp_broker
{
public:
    wamp_broker();
    ~wamp_broker();
};

} // namespace bonefish

#endif // BONEFISH_WAMP_BROKER_HPP
